# Used by docker-compose.yml (docker compose auto-loads a .env file from the
# same directory) to fill in the db/backend service credentials. Copy this to
# .env on the deploy server and set real values there — .env is gitignored,
# never commit real credentials.
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=
