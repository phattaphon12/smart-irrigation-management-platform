name: Deploy to Server

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Deploy
        run: |
          cd /home/deploy/SIMP
          git pull origin main
          docker compose up -d --build backend frontend
